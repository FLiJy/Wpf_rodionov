<Page x:Class="pr14.SessionPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Выбор места" 
      Background="#ecf0f1">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" CornerRadius="8" Padding="20" Margin="0,0,0,20">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" Opacity="0.2" ShadowDepth="3"/>
            </Border.Effect>
            <StackPanel>
                <TextBlock x:Name="txtFilmTitle" FontSize="24" FontWeight="Bold" Margin="0,0,0,10"/>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="📅 " FontSize="16"/>
                    <TextBlock x:Name="txtDate" FontSize="16" Margin="0,0,20,0"/>
                    <TextBlock Text="⏰ " FontSize="16"/>
                    <TextBlock x:Name="txtTime" FontSize="16" Margin="0,0,20,0"/>
                    <TextBlock Text="🎬 " FontSize="16"/>
                    <TextBlock x:Name="txtHall" FontSize="16" Margin="0,0,20,0"/>
                    <TextBlock Text="🎫 " FontSize="16"/>
                    <TextBlock x:Name="txtPrice" FontSize="18" FontWeight="Bold" Foreground="#27ae60"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Background="#2c3e50" Height="40" CornerRadius="8" Margin="0,0,0,30">
            <TextBlock Text="ЭКРАН" FontSize="16" Foreground="White" 
                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>

        <ScrollViewer Grid.Row="2" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Выберите место:" FontSize="18" FontWeight="Bold" 
                           Margin="0,0,0,15"/>

                <UniformGrid x:Name="seatGrid" Grid.Row="1" Margin="0,0,0,30"/>
            </Grid>
        </ScrollViewer>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20">
            <Button Content="Очистить выбор" Width="180" Height="45" FontSize="16"
                    Click="btnClear_Click"/>
            <Button x:Name="btnBuy" Content="Купить билет" Width="200" Height="45" FontSize="16"
                    Click="btnBuy_Click" IsEnabled="False"/>
        </StackPanel>
    </Grid>
</Page>